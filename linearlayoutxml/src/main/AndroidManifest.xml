<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:dist="http://schemas.android.com/apk/distribution"
          package="com.pedroabinajm.linearlayoutxml">
    <dist:module
            dist:onDemand="false"
            dist:title="@string/title_linearlayoutxml">
        <dist:fusing dist:include="true"/>
    </dist:module>

    <application>
        <activity
                android:name=".LinearLayoutXMLActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <data
                        android:scheme="linearlayout-xml"
                        android:host="anko-showroom"/>
            </intent-filter>
        </activity>
    </application>

</manifest>

